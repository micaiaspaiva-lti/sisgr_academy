version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sisgr_academy_postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sisgr_academy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7-alpine
    container_name: sisgr_academy_redis
    ports:
      - "6379:6379"
    restart: always

volumes:
  pgdata:
